{
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "console": "hardhat console",
    "migration": "hardhat run scripts/migration.js"
  },
  "dependencies": {
    "hardhat": "^2.12.0"
  },
  "bundleDependencies": [
    "hardhat"
  ],
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0"
  }
}
